import { Container } from "./styles";
import { PrimaryButton } from "../primary-button";

const ButtonShowMore = () => {
  return (
    <Container>
      <PrimaryButton>Carregar mais</PrimaryButton>
    </Container>
  );
};

export { ButtonShowMore };
